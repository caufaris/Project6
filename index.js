//Toggle function for the map button on about page
function toggle() {
  var mapa = document.getElementById("map");
  if (mapa.style.display === "none") {
    mapa.style.display = "block";
  } else {
    mapa.style.display = "none";
  }
}
